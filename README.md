# CouchPotato

CouchPotato Is a tool which embeds backdoors (rootkits, reverse shells) into a targets device with disk encryption (LUKS) by modifying the boot/EFI partition, specifically the initramfs.